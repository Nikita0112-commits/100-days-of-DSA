⏱️ Time Complexity
O(n)
Each character is added and removed at most once
n = length of string
💾 Space Complexity
O(min(n, 26)) → O(n)
Set stores at most:
26 characters (lowercase English)
or n in general
